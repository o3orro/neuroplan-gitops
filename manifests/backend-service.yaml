apiVersion: v1
kind: Service
metadata:
  name: neuroplan-backend
  namespace: neuroplan

spec:
  selector:
    app: neuroplan-backend

  ports:
    - port: 8080
      targetPort: 8080

  type: ClusterIP
