apiVersion: v1
kind: Service
metadata:
  name: neuroplan-frontend
  namespace: neuroplan

spec:
  selector:
    app: neuroplan-frontend

  ports:
    - port: 80
      targetPort: 80

  type: ClusterIP
